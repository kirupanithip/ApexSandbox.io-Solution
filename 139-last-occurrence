public static Integer findLast(List<Integer> nums, Integer target) {
Integer index=0;
Integer count=0;
//index=nums.indexof(target);

for(Integer num=0;number<nums.size();num++)
{
   if (nums[num]==target)
    {
count+=1;
index=num;
if(count==(nums.size()-1))
{
index=num;
}
}
else
{
    return = -1;
}
}
return index;
}