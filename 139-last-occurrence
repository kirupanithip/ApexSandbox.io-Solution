public static Integer findLast(List<Integer> nums, Integer target) {
Integer index=0;
Integer count=target;
Integer nCount=0;
//index=nums.indexof(target);

for(Integer num=0;num<nums.size();num++)
{
     if(nums[num]==count)
     {
         nCount=num;
         if(nCount>num)
         {
             index=num;
         }
         
     }
    else
{
    index=-1;
}

}

return index;
}