public String aOrAn(String word) {
string newWord;

if(startswith(word,a)||startswith(word,e)||startswith(word,i) ||startswith(word,O)||startswith(word,u) )
{
    return   newWord='an'+ ' ' + word ;
}
else
{
return newWord='a'+ ' ' + word;
}  //code here
}