public String aOrAn(String word) {
string newWord;

if(startwith(word,a)||startwith(word,e)||startwith(word,i) ||startwith(word,O)||startwith(word,u) )
{
    return   newWord='an'+ ' ' + word ;
}
else
{
return newWord='a'+ ' ' + word;
}  //code here
}