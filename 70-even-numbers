public List<Integer> evenNumbers(Integer n) {
   List<evenNumbers> evenLst=new List<evenNumbers>();

    for(Integer index=2;index<=n;index++)
    {  
        if((math.mod(index,2))==0) 
        {
            evenLst.add(index);
        }
    }
    return evenLst;
}
    //code here
