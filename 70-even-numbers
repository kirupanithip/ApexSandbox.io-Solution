public List<Integer> evenNumbers(Integer n) {
    List<integer> evenNumber(List<Integer>even)

Integer num=2;
for(Iteger index=0;index=n;index++)
{
    num=num+2;
even.add(num);
}

even.sort();
return even;
}
